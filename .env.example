# Postgres connection string (Neon in production; local `prisma dev` server for local dev)
DATABASE_URL=""

# Generate with: openssl rand -base64 32
AUTH_SECRET=""

# Single-user login credentials, used by prisma/seed.ts
SEED_USER_EMAIL=""
SEED_USER_PASSWORD=""

# From console.anthropic.com — used for screenshot bet-slip parsing
ANTHROPIC_API_KEY=""
